import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  map: {
    height: '100%',
  },
  androidSucks: {
    alignItems: 'center',
    paddingTop: '60%',
  },
});

export default styles;
